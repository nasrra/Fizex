{
    "tasks": [
        {
            "label": "Debug Build Linux",
            "type": "shell",
            "command": "clang -g main.c -Werror -fsanitize=address -o main $(pkg-config --cflags --libs sdl3) -lm",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}