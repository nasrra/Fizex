{
    "tasks": [
        {
            "label": "Debug Build",
            "type": "shell",
            "command": "clang -g main.c -Werror -fsanitize=address -o main -lm",
            // "command": "clang -g main.c -o main -lm",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}