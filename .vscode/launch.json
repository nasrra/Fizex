{
    "configurations": [
        {
            "name": "Debug (Win32)",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/main.exe",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "Debug Build (MSVC)"
        }
    ]
}