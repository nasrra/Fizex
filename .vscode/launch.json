{
    "configurations": [
        {
            "name": "Debug (Win32)",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/app.exe",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "Debug Build (MSVC)"
        }
    ]
}